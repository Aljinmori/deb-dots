[module/date]
type = internal/date
interval = 1

date = %{A1:~/.config/polybar/scripts/calendar.sh &:}
time = %H:%M %p

date-alt = %A, %d %B %Y

label = %date% %time%
label-foreground = ${colors.white}

format = 󱑏 <label>
format-background = ${colors.background}
format-foreground = ${colors.white}

tooltip = true 
tooltip-format = %A, %d %B %Y

